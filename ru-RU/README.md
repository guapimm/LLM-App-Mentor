🌍 Другие языки → [English](../README.md)

# AI Model Mentor (Русский)

> **Превратите вашего AI-кодинг-ассистента в осторожного full-stack наставника с 10-летним опытом — чистые промпты, ноль зависимостей.**

---

## Что это?

**Чисто-промптовая (Prompt) фреймворк-система**, которая превращает вашего AI-кодинг-ассистента в **full-stack архитектора и наставника по разработке с 10-летним опытом**, созданную для новичков в программировании с нулевой базой.

Она заставляет AI соблюдать набор «железных правил» — делая *Безопасность прежде всего, Прозрачную логику, Документацию прежде всего, Эффективность Token, Поэтапную реализацию и Контроль ресурсов* поведением по умолчанию. Результат: AI, который не просто *пишет код*, а пишет **безопасный, поддерживаемый, документированный** код.

> ⚠️ Фреймворк совместим с несколькими ИИ-инструментами: **opencode / Claude Code / Codex / Cursor и др.** Инструкции по загрузке для каждого инструмента — в [COMPATIBILITY.md](./COMPATIBILITY.md).

## Основные модули (совместимость с несколькими инструментами)

| Модуль | Файл | Назначение |
|--------|------|------------|
| 🧑‍🏫 Роль наставника | [AGENTS.md](./prompts/AGENTS.md) | Персона full-stack архитектора-наставника + 6 железных правил + чек-лист самопроверки безопасности и производительности ★ ядро, обязательно к использованию |
| 🛡️ Спецификация безопасности | [security.md](./prompts/security.md) | 8 областей безопасности: управление секретами / валидация ввода / база данных / XSS / файловая система / внешние запросы / обработка ошибок / производительность и ресурсы |
| 🎨 Стиль взаимодействия | [style.md](./prompts/style.md) | Бытовые аналогии, фазовые теги, «сначала подтверди, потом выполняй», прогрессивная сложность |
| 📋 Рабочий процесс разработки | [workflow.md](./prompts/workflow.md) | Система документации / оценка ресурсов / проектирование базы данных / протокол позиционирования фронтенда / развёртывание и аварийное восстановление / замкнутый цикл самотестирования / якоря версий |

## 📦 Другие документы

- [COMPATIBILITY.md](./COMPATIBILITY.md) — инструкция по загрузке для каждого ИИ-инструмента (opencode / Claude Code / Codex / Cursor и т.д.)
- [Полный-промпт-наставника.md](./prompts/Полный-промпт-наставника.md) — единый полный промпт (все модули объединены)

### 6 железных правил

1. **Код — это документация** — весь код несёт комментарии, объясняющие «почему»
2. **Безопасность прежде всего** — никаких хардкод-секретов, строгая валидация ввода, параметризованные запросы, защита от XSS
3. **Ноль разрушительных изменений** — сначала проанализируйте зависимости, помечайте правки как 【Обязательное изменение】/【Опциональная оптимизация】
4. **Поэтапное выполнение** — не более 300 строк за один вывод, на каждом шаге — ожидание подтверждения
5. **Модульная изоляция** — не более 500 строк на файл, оставляйте интерфейсы расширения
6. **Производительность и ресурсы — заранее** — синхронно с проектированием базы данных выводите план индексов; интерфейсы запросов по умолчанию используют пагинацию; на начальном этапе проекта выполняйте оценку ресурсов по трём категориям (память / диск / вычислительная мощность); для операций с большим потреблением памяти обязателен механизм освобождения ресурсов

## ⬇️ Установка и использование mentor CLI

**Способ A: бинарный файл Go (рекомендуется, без зависимостей, кроссплатформенно)**

Скачайте исполняемый файл `mentor` для вашей платформы (v0.1.0, поддерживает Windows / Linux / macOS) со страницы Releases на GitHub и добавьте его в PATH:

```bash
mentor install                        # интерактивный мастер: выбор языка → выбор модулей (по умолчанию agent) → автоопределение инструмента
mentor install --lang zh-CN --modules agent,security --cli claude-code --dir ./proj
mentor add workflow                   # добавить модуль
mentor list                           # показать установленные модули
mentor detect                         # определить AI-инструмент, используемый в проекте
mentor pack                           # сгенерировать совместимый каталог skill
```

`mentor` автоматически записывает файл с правильным именем и в правильное место в зависимости от инструмента: opencode/Codex → `AGENTS.md`, Claude Code → `CLAUDE.md`, Cursor → `.cursor/rules/`.

**Способ B: копирование вручную**

Следуя инструкциям в [COMPATIBILITY.md](./COMPATIBILITY.md), скопируйте файлы из `prompts/` в соответствующие места вашего проекта.

> Поддерживаемые команды: `install` / `add` / `remove` / `list` / `detect` / `pack`; модули: agent (по умолчанию) / security / style / workflow / complete; инструменты: opencode / claude-code / codex / cursor / other.

## 🧩 Подключение IDE через MCP (подсказки по требованию)

Пакет `mentor-mcp` опубликован на npm: **[npmjs.com/package/mentor-mcp](https://www.npmjs.com/package/mentor-mcp)**. Полное руководство: [mcp/README.md](../mcp/README.md).

- **Рекомендуется (npm):** без скачивания — запускайте напрямую:

```bash
npx mentor-mcp
# или установите глобально: npm install -g mentor-mcp
```

- **Офлайн-релиз:** скачайте `guapimm-mentor-mcp-*.tgz` со страницы [Releases](https://github.com/guapimm/AI-Model-Development-Mentor/releases) (самодостаточный, без `tsc`).
- **Из исходников (для разработчиков):**

```bash
git clone https://github.com/guapimm/AI-Model-Development-Mentor.git
cd AI-Model-Development-Mentor/mcp
npm install
npm run build
```

В конфигурации MCP укажите команду `npx mentor-mcp` (сборки из исходников используют **абсолютный путь** к `mcp/dist/index.js`). Первый вызов инструмента: `session_start`.

## 📖 Руководство по использованию (opencode)

### Команды (шпаргалка)

| Сценарий | Действие |
|------|------|
| Ежедневная разработка | Войдите в проект → opencode автоматически загружает AGENTS.md → обычный диалог |
| Долгосрочный проект | Храните правила в AGENTS.md (обновляйте через `/init`) |
| Неожиданный разрыв связи | Восстановитесь через `opencode --continue`, правила сохранятся |
| Намеренно новое открытие сессии | Просто запустите `opencode` — AGENTS.md загрузится автоматически |

### Структура файлов проекта

```
📁 my-project/
├── 📄 AGENTS.md          ← главный промпт
├── 📄 security.md        ← правила безопасности
├── 📄 workflow.md        ← правила рабочего процесса
├── 📄 style.md           ← стиль взаимодействия
└── 📁 src/
```

---

### Конкретные сценарии

#### Сценарий 1: повседневное написание кода (загружен только AGENTS.md)

> Вы: «Помоги мне написать API для получения списка пользователей»

Нужно загрузить: AGENTS.md (уже загружен автоматически, действия не требуются)

AI автоматически:

- Код с комментариями на русском языке
- Отмечает чек-лист безопасности перед выводом
- Выполняет пошагово (≤300 строк)
- Один файл ≤500 строк

#### Сценарий 2: написание API входа/регистрации (загрузить AGENTS.md + security.md)

> Вы: «Помоги мне написать функцию входа пользователя, сделай по требованиям security.md»

Нужно загрузить:

```bash
@security.md
```

AI дополнительно:

- Хранит пароли в виде хеша bcrypt
- Устанавливает время истечения для JWT Token
- Защита от brute force (ограничение неудачных попыток входа)
- Защита от SQL-инъекций (параметризованные запросы)

#### Сценарий 3: запуск проекта с нуля (загрузить AGENTS.md + workflow.md)

> Вы: «Я хочу сделать блог-систему, помоги мне собрать каркас проекта по workflow.md»

Нужно загрузить:

```bash
@workflow.md
```

AI дополнительно:

- Создаёт docs/architecture.md (выбор технологического стека + схема архитектуры)
- Создаёт docs/dev_log.md (шаблон журнала разработки)
- Создаёт docs/api_interface.md (шаблон контрактов интерфейсов)
- Создаёт docs/SNAPSHOT.md (снимок проекта)
- Генерирует скрипты backup.sh и rollback.sh

#### Сценарий 4: AI объясняет слишком запутанно (загрузить style.md)

> Вы: «Объясни мне, что такое JWT, в стиле style.md, с помощью бытовых аналогий»

Нужно загрузить:

```bash
@style.md
```

AI дополнительно:

- Объясняет JWT через «карту постоянного клиента ресторана»
- Добавляет фазовый тег [📋 Анализ требований]
- Сначала даёт вывод, потом детали
- Предлагает 2–3 варианта решения

#### Сценарий 5: развёртывание и запуск (загрузить AGENTS.md + workflow.md)

> Вы: «Напиши мне Docker-конфигурацию для развёртывания по правилам workflow.md»

Нужно загрузить:

```bash
@workflow.md
```

AI дополнительно:

- Разделяет конфигурации окружений разработки и production
- Генерирует docker-compose.yml
- Генерирует health_check.sh
- Напоминает о шагах резервного копирования и отката

### ⚠️ Когда НЕ загружать?

| Когда не нужно загружать | Причина |
|---------------|------|
| Чисто технический вопрос (например, «как использовать React useEffect») | AGENTS.md достаточно, добавление workflow только мешает |
| Правка одного CSS-стиля | Не нужны правила безопасности и процесс развёртывания |
| Попросить AI перевести текст | Никакой модуль вообще не нужен |
| Простой рефакторинг существующего кода | Чек-лист безопасности AGENTS.md уже покрывает |

### 💡 Итог в одной фразе

> AGENTS.md — это скин по умолчанию, а остальные три — эффектные плагины: включайте их только когда нужно, в обычное время не включайте — экономия Token и чистота.

## Быстрый старт (3 шага)

```bash
# 1. Скопируйте роль наставника в свой проект (переименуйте в AGENTS.md)
cp prompts/AGENTS.md AGENTS.md

# 2. (Рекомендуется) Добавьте также спецификации безопасности/стиля/рабочего процесса
cp prompts/security.md security.md
cp prompts/style.md style.md
cp prompts/workflow.md workflow.md
```

3. Запустите opencode и скажите:

> «Я полный новичок. Вот моя 【Спецификация требований к проекту】: название проекта ____, основная цель ____, роли пользователей ____, основные операции ____, обязательные к хранению данные ____. Начните с Этапа 0: подготовка окружения и выбор технологического стека и ведите меня пошагово.»

AI будет продвигаться по схеме «Дизайн → Основная логика → Интерфейс → Тестирование», ожидая вашего подтверждения на каждом этапе.

## FAQ

**В: Нужны ли мне все 4 модуля?**
О: Нет. `AGENTS.md` — единственный обязательный. Добавьте `security.md` для более надёжной защиты, `style.md` для более дружелюбного общения.

**В: Работает ли это с другими AI-продуктами?**
О: Да. Содержимое промптов не привязано к конкретному инструменту, поэтому фреймворк совместим со всеми популярными инструментами — opencode, Claude Code, Codex, Cursor и др. Инструкции по загрузке для каждого инструмента см. в [COMPATIBILITY.md](./COMPATIBILITY.md).

**В: Это переведено машинным способом?**
О: Все языковые версии — это выверенные переводы одного китайского оригинала. Если вы заметили неточность, смело открывайте issue или PR.

## Лицензия

[Apache-2.0 License](../LICENSE) © 2026 guapimm
